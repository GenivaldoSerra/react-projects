function Primeiro () {
    const msg = 'Seja bem vindo(a)!';
    return (
        <div>
            <span>Primeiro Componente!</span>
            <h2>{ msg }</h2>
        </div>
    );
}

export default Primeiro